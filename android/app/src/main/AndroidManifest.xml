<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:largeHeap="false"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <!-- Capacitor's local web server, which also serves videos from
         app-specific external storage to the WebView. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Push-to-talk. Requested at runtime the first time the user holds the
         microphone button; also contributed by the speech-recognition plugin. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
      No storage permission is declared on purpose.

      IRONBOX keeps its videos in app-specific external storage
      (Android/data/com.ironbox.virtualassistant/files/IRONBOX/videos), which an
      app may read and write without any permission on every supported Android
      version. Broad media/storage permissions would only be needed to reach
      shared folders, which IRONBOX does not use; the in-app "Import Videos"
      picker covers that case instead.
    -->

    <!-- The device speech and TTS services this app talks to. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>
</manifest>
